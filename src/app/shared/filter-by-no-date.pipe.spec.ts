import { FilterByNoDatePipe } from './filter-by-no-date.pipe';

describe('FilterByNoDatePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByNoDatePipe();
    expect(pipe).toBeTruthy();
  });
});
